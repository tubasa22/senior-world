document.querySelectorAll('.nav-dropdown-btn').forEach(button=>{const dropdown=button.closest('.nav-dropdown');const close=()=>{dropdown.classList.remove('open');button.setAttribute('aria-expanded','false')};button.addEventListener('click',event=>{event.stopPropagation();const opening=!dropdown.classList.contains('open');document.querySelectorAll('.nav-dropdown-btn').forEach(other=>{other.closest('.nav-dropdown').classList.remove('open');other.setAttribute('aria-expanded','false')});if(opening){dropdown.classList.add('open');button.setAttribute('aria-expanded','true')}});dropdown.addEventListener('keydown',event=>{if(event.key==='Escape'){close();button.focus()}})});document.addEventListener('click',()=>document.querySelectorAll('.nav-dropdown-btn').forEach(button=>{button.closest('.nav-dropdown').classList.remove('open');button.setAttribute('aria-expanded','false')}));document.addEventListener('keydown',event=>{if(event.key==='Escape')document.querySelectorAll('.nav-dropdown-btn').forEach(button=>{button.closest('.nav-dropdown').classList.remove('open');button.setAttribute('aria-expanded','false')})});
