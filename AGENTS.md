# AGENTS.md — 이 저장소에서 작업하는 AI 에이전트를 위한 규칙

이 파일은 Codex 등 코딩 에이전트가 이 저장소에서 작업을 시작하기
전에 반드시 먼저 읽어야 하는 운영 규칙이다. README.md는 사람을 위한
설명 문서이니 참고용으로 함께 읽되, 아래 규칙이 우선한다.

## 0. 현재 상태 확인부터

작업을 시작하기 전에 README.md 12장(AI 세션 간 인수인계 노트)을
먼저 읽고, git log로 실제 merge된 커밋과 상태 표시(✅/🔄/📋)가
일치하는지 확인한다. 인수인계 노트가 최신이 아닐 수 있으므로
git log가 항상 우선한다.

## 1. 이 프로젝트의 핵심 원칙 (README 1장과 동일, 여기서도 강제)

- 데이터와 화면 분리: 아파트 데이터는 HTML에 박지 않고 data/의 JSON으로
- 스타일은 assets/theme.css 한 곳에서만 관리 (마이그레이션 전까지는
  각 HTML의 :root 블록 값만 동일하게 유지)
- 페이지는 얇게, 무거운 로직은 assets/의 JS로
- 정적 우선(static-first), 백엔드는 필요할 때만
- 기존 파일을 갈아엎지 말고 점진적으로 확장

## 2. 데이터 정확성 — 이 저장소에서 가장 중요한 규칙

이 사이트는 실제 시니어들이 주거 정보를 얻는 데 쓰인다.
**허위/미검증 데이터는 실질적 피해로 이어진다.**

- 아파트 데이터(이름, 주소, 전화번호, 연령 제한, 세대수)를
  임의로 지어내거나 추측으로 채우지 않는다. 확인 안 된 필드는
  null 또는 "확인 필요"로 명시한다.
- 새 데이터셋을 추가할 때는 반드시 출처를 DATA\_SOURCES.md에 기록한다.
- 데이터 출처가 "이번 달 신청 가능" 같은 시점성 정보(예: KIWA 월간
  목록)인 경우, 영구적인 건물 목록(OC 143건 같은)과 절대 같은
  배열/파일에 섞지 않는다. 별도 파일로 유지하고 갱신 주기를
  README/DATA\_SOURCES.md에 명시한다.
- 제3자가 취합한 데이터(예: KIWA의 월간 PDF)를 사이트에 정식
  게시하기 전에는 재게시 허락 여부를 사람이 확인해야 한다는 점을
  작업 결과에 명시한다 — 이건 코드로 자동 해결되는 문제가 아니다.

## 3. 코드 수정 시 규칙

- CSS 변수는 이름을 바꾸지 않는다. 값만 교체한다 (수십 곳에서
  var(--teal) 등으로 참조하므로, 이름을 바꾸면 참조도 전부
  바꿔야 하는 리스크가 생긴다).
- 정규식/파싱 로직을 수정할 때는 재현 케이스(실패했던 실제 데이터)를
  테스트해서 결과를 diff 설명에 남긴다.
- window\.storage, 기타 Claude 아티팩트 전용 API는 절대 사용하지
  않는다 — 실제 배포 환경(GitHub Pages)에서 존재하지 않는다.
  브라우저 표준 API(localStorage 등)만 사용한다.
- Nominatim 등 외부 지오코딩 API는 이용약관(초당 요청 제한 등)을
  반드시 지킨다. 대량 조회가 필요하면 US Census Batch Geocoder
  같은 배치 API를 우선 검토한다 (scripts/geocode-batch.mjs 참고).
- .bak, .tmp 등 백업/임시 파일은 .gitignore에 등록하고 커밋하지 않는다.

## 4. 작업 절차

- 모든 변경은 커밋을 나누고, 커밋 메시지는 "타입: 한국어 설명"
  형식(fix:, feat:, docs:, style:, a11y: 등)을 따른다.
- 코드를 직접 merge하지 않는다. diff를 만들어 사람(리뷰어)에게
  제시하고 승인을 기다린다.
- 큰 작업(여러 파일에 걸친 구조 변경 등)은 단계를 나누어 각 단계마다
  화면/기능이 정상 동작하는지 확인 가능하게 만든다 (README 7장
  마이그레이션 원칙과 동일).
- 접근성 관련 수정(aria-label, 색 대비 등)을 하면 결과를
  구체적 수치(Lighthouse 점수, 대비율 등)로 diff 설명에 남긴다.

## 5. 이 저장소의 히스토리를 알고 싶으면

README.md 11장(수정 이력)과 12장(인수인계 노트)을 참고한다.
이 두 장이 지금까지의 모든 리뷰-구현 사이클을 기록한 곳이다.
